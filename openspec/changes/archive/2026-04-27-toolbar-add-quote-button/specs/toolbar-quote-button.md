# Toolbar Quote Button

## Overview

工具栏添加"引用"按钮，用于快速插入 Markdown 引用语法。

## Behavior

1. 无选中文本时点击
   - 在当前行首插入 `> `

2. 有选中文本时点击
   - 选中文本前后包裹 `> `

## UI

- 图标：Lucide React `Quote`
- Tooltip：`引用 (Quote)`
- 位置：水平线按钮之前
